// Import des composants nécessaires de React Native et React
import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

/**
 * Composant HomeProduct
 * Composant fonctionnel React qui affiche la section produit de la page d'accueil
 * @returns JSX.Element - Rendu du composant HomeProduct
 */
const HomeProduct: React.FC = () => {
  return (
    <View>
      <Text>HomeProduct</Text>
    </View>
  )
}

// Export du composant pour utilisation dans d'autres fichiers
export default HomeProduct

// Définition des styles (actuellement vide, à compléter selon les besoins)
const styles = StyleSheet.create({})